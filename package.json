{
  "name": "@robotixai/harness-root",
  "private": true,
  "description": "Provider-agnostic model harness — single interface for Anthropic, OpenAI, Ollama, and OpenRouter",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "turbo": "^2.5.0",
    "typescript": "^5.8.3",
    "vitest": "^1.6.0"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
